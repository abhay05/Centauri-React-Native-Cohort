/**
 * @format
 */

import { AppRegistry } from 'react-native';
import EmailInbox from './App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => EmailInbox);
